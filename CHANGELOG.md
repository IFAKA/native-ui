# Changelog

All notable changes will be documented here. Releases follow Semantic Versioning after the public API is declared stable.

## Unreleased

- Initialized the Native UI 1.0 specification and repository foundation.
- Added the bounded `--nui-*` visual token foundation with light/dark, contrast,
  forced-colors, and reduced-motion contracts.
- Added the dependency-light documentation site and public-vocabulary contract tests.
- Added the Task 13 beta evidence report and convergence record, with external
  publication and user-study limitations stated explicitly.
- Added the deterministic `native-ui analyze` and `native-ui explain` authoring analyzer.
- Added offline, provenance-aware upstream research adapters with explicit manual promotion gates.
- Reconciled the foundation task tracker and requirements checklist against the
  checked-in implementation and evidence.
- Exposed recommendation-only semantic inference through the CLI and manifest,
  and added inference coverage to deterministic benchmark reports.
- Added the framework-free Native UI public site assembler, manifest-backed
  discovery pages, comparison lab, evidence routes, and static Vercel config.
- Corrected analyzer name detection for visibly labelled native links and
  buttons exposed by the public-site dogfood pass.
