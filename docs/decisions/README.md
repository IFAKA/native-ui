# Architecture Decision Records

Create one immutable, numbered Markdown record for decisions affecting public API, browser support, dependencies, accessibility behavior, packaging, inference, or releases.

Use:

```text
0001-short-title.md
```

Each record contains status, context, decision, alternatives, consequences, validation, and rollback/revisit conditions.
